# HTML 뷰
웹 브라우저 컨트롤을 기반으로 하는 뷰
# 사용 
1. Navigate(), Navigate2() -> URL 로 지정한 웹 문서 또는 로컬 파일을 연다.
2. GoHome() -> 인터넷 익스플로러에서 설정한 시작 페이지로 이동한다.
3. GoBack() -> 히스토리 목록에서 이전 항목으로 이동한다.
4. GoForward() -> 히스토리 목록에서 다음 항목으로 이동한다.
5. Refresh(), Refresh2() -> 현재 표시하고 있는 내용을 다시 로드한다.
6. Stop() -> 현재 작업을 중지한다.